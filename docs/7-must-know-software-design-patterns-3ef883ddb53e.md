# 7 必须了解软件设计模式

> 原文：<https://levelup.gitconnected.com/7-must-know-software-design-patterns-3ef883ddb53e>

## 中的例子来探究这些模式。网

![](img/13b9d07af03e62268bb05db3d842976d.png)

由 [Unsplash](https://unsplash.com/photos/pjAH2Ax4uWk) 上 [charlesdeluvio](https://unsplash.com/@charlesdeluvio) 拍摄的照片

## 为什么我们应该关心设计模式

简而言之，设计模式通过创建一个可重用的解决方案来帮助我们解决问题，我们可以将该解决方案用作我们软件的模板。也就是说，设计模式不是算法，你不能把它们粘贴到代码库中。它们给了你一个模板，但是如果误用，一些模式可能会导致额外的复杂性，这不是它们的本意。

关于设计模式有很多争议——它们中的一些是否已经过时，或者它们经常被过度使用，这导致了我们提到的额外的复杂性，但事实是其中相当多的模式非常重要，为了不误用或过度使用它们，你需要完全理解它们。

有 3 种类型的设计模式:

*   创造型的
*   结构的
*   行为的

我们将讨论每一个的类型，所以不再多说，让我们开始吧。

## 1.一个

> 创造模式

singleton 模式用于限制一个类只能创建一个对象。使用这种模式的一个很好的例子是当你只需要一个不需要太多改变的类的单一实例时。日志记录、缓存服务。我们可以做到这一点的方法是将构造函数**私有**，这将阻止我们使用“new”关键字，而是检查是否已经存在该类的实例——如果没有，我们可以创建一个。

## 2.工厂方法

> 创造模式

你知道现实生活中的工厂是如何生产多种产品的——例如，食品工厂可以生产各种美味的食品。嗯，软件工厂可以创建各种各样的新类。当然，您确实有能力自己创建一个类，但是它将与我们代码的具体实现紧密耦合。

我给你举个例子。

我们有两个独立的类，当我们想创建其中一个时，我们必须用 if 语句把整个东西包装起来。对于那些不知道欧宝在英国有一个不同的名字相比，原来的德国名字-欧宝。

那么，对此有什么更好的解决方案呢？

嗯，事情可能是这样的，汽车工厂将为我们检查国家信息，并返回符合我们标准的汽车，我们的代码不会紧密耦合。

*！这是伪代码，所以如果你尝试它，它不会工作。我们只需要观想不同之处。*

## 3.观察者

> 行为模式

这种模式是一种依赖关系，允许许多对象观察单个对象，换句话说，就是对象之间的一对多依赖关系。

因此，如果主对象进行了更新，所有的观察者都会意识到这一点，并采取相应的行动。

这个需要更多的代码来可视化，所以我不打算在这里这样做，但为了简单起见，让我们想象我们在 Instagram，Twitter 或任何其他社交平台上，你可以关注/订阅人们。基本上发生的是，你观察那个人，当他发布/推文时，你也会收到更新。

我还写了一篇关于消息代理的文章，使用了相同的概念，如果你感兴趣，你可以通过下面的链接查看。

[](https://blog.devgenius.io/message-broker-architecture-what-is-it-and-how-does-it-work-60a62f8ce784) [## 消息代理架构，它是什么，如何工作

### 什么是队列、主题和订阅

blog.devgenius.io](https://blog.devgenius.io/message-broker-architecture-what-is-it-and-how-does-it-work-60a62f8ce784) 

## 4.建设者

> 创造模式

顾名思义，构建器模式用于构建对象。有时创建对象可能很复杂。我的意思是，是的，你可以使用一个构造函数来传递需要的值，但是这样会很快变得很难看。比如说。

假设我们有了这个汽车类，并创建了一个这样的新对象。

那里只有 3 处房产，当我在打新车的时候，我已经忘记了订单。

那么，怎样才能更干净一点呢？嗯，我们可以稍微重构一下汽车类。

这将允许我们单独设置每个属性，并且我们可以重构我们的生成器:

这更整洁，可读性更强。

## **5。适配器**

> 结构模式

适配器模式相当简单。它允许不兼容的类一起工作。其实现方式是将一个类的接口转换成另一个类的接口——就像翻译器一样。

我们有一个基本的 EnglishGreeting()函数，其中包含一个 Greeting()函数。我们还有一个 TranslatedGreeting()从初始类继承了 Greeting()函数。通过创建一个适配器，我们允许自己在 TranslatedGreeting()中应用一些不同的逻辑，并显示不同于“Hello”的内容。

## 6.调解人

> 行为模式

对于这一个，我不会用代码来烦你，因为它可能会有点冗长，但是我们可以把中介器看作某种中间件。假设你从宜家买了一些家具，有各种不同的螺钉需要安装在特定的地方。把螺丝钉想象成一个物体——它是同一个物体，但是有一些不同的属性(大小等等)。).

如果那是软件，中介会做什么来帮助我们管理哪个螺丝适合哪里，让我们的生活变得更轻松。

## 7.代理人

> 结构模式

代理设计模式所做的是允许我们为目标类创建一个替代品，让我们可以通过替代品控制类。

例如，如果你的学校老师生病了，你可能会有一个代课老师，但你的正式老师很可能会控制她的行为，例如，她会告诉代课老师在她不在的时候教你什么。

就像这样，我们可以创建一个代理(代理教师)，然后用它来控制教师的任务(讲课)。

## 结论

尽管您可能不需要在日常工作中记住这些设计模式，但至少对它们有一个公平的理解并知道它们的存在是有好处的。也有相当多的，我已经离开了，所以我会建议去看看他们的一般知识。

除此之外，你也可以看一看坚实的原则，因为它们通常在找新工作和准备面试时同样重要。请查看以下内容:

[](https://medium.com/codex/solid-principles-in-c-df43187697f4) [## C#中的坚实原则

### 坚实的原则

在 C#实心 Principlesmedium.com 中](https://medium.com/codex/solid-principles-in-c-df43187697f4) [](https://medium.com/@ivan.zstoev/membership) [## 通过我的推荐链接加入 Medium-Ivan Stoev

### 作为一个媒体会员，你的会员费的一部分会给你阅读的作家，你可以完全接触到每一个故事…

medium.com](https://medium.com/@ivan.zstoev/membership) 

# 分级编码

感谢您成为我们社区的一员！更多内容见[级编码出版物](https://levelup.gitconnected.com/)。

关注:[推特](https://twitter.com/gitconnected)、[领英](https://www.linkedin.com/company/gitconnected)、[时事通讯](https://newsletter.levelup.dev/)

**升一级正在改造理工大招募➡️** [**加入我们的人才集体**](https://jobs.levelup.dev/talent/welcome?referral=true)
# 结构化、非结构化和半结构化数据

> 原文：<https://levelup.gitconnected.com/structured-unstructured-semistructured-data-259b869725ca>

## 了解结构化、半结构化和非结构化数据之间的区别

![](img/bbf8869d6740330c324feba10a521c9b.png)

汉斯-彼得·高斯特在 [Unsplash](https://unsplash.com/s/photos/chaos?utm_source=unsplash&utm_medium=referral&utm_content=creditCopyText) 上拍摄的照片

## 介绍

据估计，仅在 2021 年就有超过 **2.5 万亿字节的数据**被**生成**，而超过 **79 万亿字节的数据**被**消耗**(来源:Statista)，而这些数字每年都在呈指数级增长。但是你可能会有一个问题，当提到术语**数据**时，我们真正的意思是什么？

数据有不同的类型；从电子表格到文本和视频甚至音频文件。在今天的文章中，我们将讨论**结构化**、**非结构化**和**半结构化**数据之间的主要区别，并给出一些真实世界的例子。此外，我们将讨论每种数据类型应该存储在组织中的什么位置，以便建立和维护适当的数据管理策略。

## 结构数据

结构化数据是一种在被存储到存储装置之前被期望具有某种预定义的结构的数据。这种结构通常被称为**写模式**。

这种类型的数据可以是人工生成的，也可以是机器生成的。结构化数据最简单的例子是分析师创建的电子表格(人工生成)。机器生成的结构化数据示例包括网络日志，或基于事件创建的数据条目(例如，当客户购买产品时，新的销售条目被创建，包含价格、数量和与该特定购买相关的潜在的更多字段)。生成结构化数据的系统的其他示例包括预订或库存控制系统。

**结构化数据通常驻留在关系数据库管理系统(RDBMS)中**。关系数据库通常由许多表组成，其中每个表都有一个预定义的模式，每个记录都必须满足该模式。例如，每个字段与预期的数据类型相关联(例如，姓名字段预期是特定长度的字符串类型)。然后，可以对存储的数据执行进一步的查询，以便检索与指定条件匹配的记录。

## 非结构化数据

非结构化数据是指无法存储在关系数据库中的数据，因为它们缺少预定义的数据模型。这些数据直到被实际使用时才被处理。这个概念也被称为**读模式。**

被视为非结构化的数据类型包括视频或音频文件、文本、网站、演示文稿、从各种传感器收集的数据，甚至是卫星图像。

**非结构化数据通常** **驻留在组织内的数据湖**。非结构化数据的另一种存储方式包括**非关系数据库，如 NoSQL** 。

在组织内使用非结构化数据的用户包括数据、机器学习和人工智能科学家和工程师，他们应用特定的技术来从这种类型的数据中提取意义(甚至某种高级结构)。

## 半结构化数据

现在，半结构化数据是指一种特定类型的数据，这种数据实际上是非结构化的，但同时它也包含某种形式的元数据，使用户能够确定某种**部分结构**或**层次结构**。

举个例子，考虑一下 **XML 标记语言**。XML 是一组编码规则的集合，这些规则以人类和机器可读的形式应用于文档。每一条数据都被输入到标签中，使用户能够访问特定级别的特定信息。其他常用的半结构化数据包括电子邮件和 JSON (JavaScript 对象符号)。

**现代 NoSQL 数据库**如 Couchbase 和 MongoDB 能够以 JSON 格式存储半结构化数据。

## 结构化数据和非结构化数据的区别

除了结构化数据和非结构化数据在模式方面的明显差异(或者在非结构化数据的情况下缺乏模式)之外，结构化数据的最大优势是**易于处理**。现代分析工具(通常由业务、洞察、BI 或数据分析师和科学家使用)主要用于处理结构化数据。

如前所述，数据挖掘技术通常由 ML 和数据科学家应用，以便从非结构化数据中提取有用的信息，因为这种类型的数据占通常驻留在组织内的数据的 80%以上。因此，对于企业来说，处理和提取非结构化数据非常重要，否则他们将丢掉大量有价值的信息，这些信息可能有助于他们做出更好、更及时的数据驱动型决策。

## 最后的想法

在今天的文章中，我们讨论了不同类型的数据，即结构化、非结构化和半结构化数据。我们已经看到了这些类型的一些真实例子，并讨论了它们在现代组织中的主要用途。

结构化数据通常存储在数据库中(数据库又包含在数据仓库中)。另一方面，非结构化数据存储在数据湖或非关系数据库中，因为它们没有任何预先定义的模式来满足先验需求。最后，半结构化数据是具有某种形式的结构或层次结构(元数据)的非结构化数据，通常存储在 MongoDB 等现代 NoSQL 数据库中。

[**成为会员**](https://gmyrianthous.medium.com/membership) **阅读介质上的每一个故事。你的会员费直接支持我和你看的其他作家。你也可以在媒体上看到所有的故事。**

[](https://gmyrianthous.medium.com/membership) [## 通过我的推荐链接加入 Medium-Giorgos Myrianthous

### 作为一个媒体会员，你的会员费的一部分会给你阅读的作家，你可以完全接触到每一个故事…

gmyrianthous.medium.com](https://gmyrianthous.medium.com/membership) 

**你可能也会喜欢**

[](https://towardsdatascience.com/data-lakes-vs-data-warehouses-47444228604c) [## 数据湖与数据仓库

### 数据湖和仓库有什么区别？

towardsdatascience.com](https://towardsdatascience.com/data-lakes-vs-data-warehouses-47444228604c) [](https://pub.towardsai.net/data-versioning-for-efficient-workflows-with-mlflow-and-lakefs-892df1f8e7d8) [## 使用 MLFlow 和 LakeFS 实现高效工作流的数据版本控制

### 构建弹性、原子和版本化的数据湖操作

pub.towardsai.net](https://pub.towardsai.net/data-versioning-for-efficient-workflows-with-mlflow-and-lakefs-892df1f8e7d8) [](https://pub.towardsai.net/object-storage-521d5454d2d) [## 对象存储对于大数据是否高效？

### 使用对象存储来存储、归档和管理数据

pub.towardsai.net](https://pub.towardsai.net/object-storage-521d5454d2d)
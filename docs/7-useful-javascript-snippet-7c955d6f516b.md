# 7 个有用的 JavaScript 代码片段

> 原文：<https://levelup.gitconnected.com/7-useful-javascript-snippet-7c955d6f516b>

## 学习这些可以派上用场的激动人心的片段

![](img/5385e781620d3bc7b6070cfd35f492c1.png)

由[约书亚·阿拉贡](https://unsplash.com/@goshua13?utm_source=medium&utm_medium=referral)在 [Unsplash](https://unsplash.com?utm_source=medium&utm_medium=referral) 拍摄

JavaScript 是最常用的语言之一。它是一种脚本语言，主要用于 web 应用程序开发。在文章中，学习一些有用的片段。

# 1.删除列表中的重复项

> ***编写一个 JavaScript 程序，从一个列表中复制并显示列表的内容。***

有不同的方法可以删除列表中的重复项。将一个索引转换成一个集合可以很容易地删除重复项。代码中有一个包含重复值的列表，代码的输出是唯一元素的列表。

# 2.在注释后删除任何内容

> ***写一个 JavaScript 程序，把注释后面那一行的东西都剥去。***

我们使用两个斜线“//”来注释 JavaScript 中的一行。当在代码或文本前面使用该符号时，编译器会自动忽略它。这个程序将帮助删除程序中的注释行。一个简单的输入被认为带有一个注释符号。

# 3.检查对象中是否存在属性

> ***这是一段 JavaScript 代码，用于检查对象中是否存在属性。***

在某些情况下，检查对象的属性是必要的。当你在没有打字稿的情况下工作时，这是必要的。代码返回一个布尔值。如果属性存在，则返回 true 或 false。下面的代码将首先打印 true 和 false，表明对象中不存在“Experience”。

# 4.用 JavaScript 播放音频

> ***写一个简单的 JavaScript 代码来播放音频。***

播放 mp3 文件声音的简单代码片段。用于播放音频的语法是`audio_object = new Audio(url).`Audio 构造函数创建并返回用户可以收听音频的元素。

# 5.将单词大写:

> ***用 JavaScript 写一个单词大写的程序。***

不可思议的是，JavaScript 没有内置的大写单词的功能。但是，有一个简单的方法可以解决这个问题。首先在代码中，我们将单词改为小写。然后第一个字母变成大写字母，并连接其余的字母。

# 6.像专业人士一样交换

> ***写一个简单的程序，在 JavaScript 中交换两个变量。***

交换是一项简单的任务，不需要很多行代码。在这个简单的代码中，我们给 x 和 y 赋了两个值。代码输出 x 和 y 的反码值。

# 7.搜索和替换:

> ***写一个程序，用 JavaScript*** *搜索一个单词并用另一个单词替换。*

一个简单的代码用另一个单词替换一个特定的单词写在下面。在代码中，我们将 web 单词改为 Medium。

请考虑通过我的链接订阅 Medium。这将有助于、支持和鼓励。谢谢

[](https://swathiarun63.medium.com/membership) [## 通过我的推荐链接加入媒体

### 作为一个媒体会员，你的会员费的一部分会给你阅读的作家，你可以完全接触到每一个故事…

swathiarun63.medium.com](https://swathiarun63.medium.com/membership)
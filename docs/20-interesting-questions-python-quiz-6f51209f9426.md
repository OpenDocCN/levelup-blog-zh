# 20 个有趣的问题 Python 测验

> 原文：<https://levelup.gitconnected.com/20-interesting-questions-python-quiz-6f51209f9426>

## 让人思考的问题，试着把它做对

![](img/d2ae6257253b36ae0ffdcded75fe6686.png)

由[萨法尔萨法罗夫](https://unsplash.com/@codestorm?utm_source=medium&utm_medium=referral)在 [Unsplash](https://unsplash.com?utm_source=medium&utm_medium=referral) 上拍摄的照片

每个程序员的逻辑推理都有助于他们创建好的项目。这里有几个问题，可以帮助分析你的逻辑思维。Python 编程是一个庞大的主题，我们往往会忘记一些概念，希望这篇文章将有助于修改一些令人兴奋的主题。

> 首先，回答问题，然后研究解决方案和解释。

## 问题 1:列表复制

**答案:C**

当您在列表上使用`=`操作符时，会出现列表的浅层副本。使用浅层复制机制，一个列表中的更改也会反映在另一个列表中。问题中，list2 是 list1 的海市蜃楼；对列表 1 的修改将反映在列表 2 中。尝试使用 copy()获得不同的结果。

## 问题 2:下划线分隔符

**答:答**

下划线是分隔符，主要用于表示数字以增加可读性。python 语法不承认代码中的下划线。这很像逗号分隔一组数字。我们打印 x 的浮点值；C 选项不是答案。

## 问题 3:默认值

**回答:D**

当你在一个函数定义中给出一个缺省值时，当你不提供输入时，该值将被考虑。在问题中，num1 被赋予一个默认值。如果不为 num1 提供输入，那么函数定义中的默认值将被视为输入。通过输入一次来尝试相同的代码。

## 问题 4:解包列表

**答:答**

给出了一个列表，它等同于要解包和赋值的变量。如果有相等的变量和列表元素，那么解包是可能的。否则它将显示一个错误。在这个例子中，我们有三个变量等同于各自的列表元素。

## 问题 5:评论

**答案:B**

有时简单的代码可能会令人困惑。为了在 Python 中注释一行，我们使用#字符。一旦使用哈希，编译器就会忽略整行。一个之后，整行(#2#3)将被认为是程序中的 Python 注释。

## 问题 6:元组添加

**答案:B**

当创建两个元组时，它被创建为两个不同的对象。并且当`+`操作符与元组一起使用时，元组的连接发生。在该示例中，两个元组的串联产生选项 b。

## 问题 7:字符串连接

**答案:C**

使用双引号时，变量是字符串数据类型。在两个字符串之间使用`+`运算符时，会导致连接。请注意，在程序中，chocolate 变量的类型被转换为 int——在该变量被转换为 int 数据类型并加上 5 之后，第一个字符串连接的结果是 50。

## 问题 8:类型

**答案:C**

执行从该行的内括号到外括号开始。除法运算首先在程序中进行，结果是(0.5)。值的类型是 float。

## 问题 9:字符串格式

**答案:B**

程序中使用了最常用的 f 字符串格式样式。这种格式主要用在 print 语句中，以增加可读性。值得注意的是，这也可以将数据存储到变量中。

## 问题 10:字符串方法

**答案:C**

在程序中，我们有两个字符串方法，大写和标题。大写字符串方法只将句子中的第一个单词大写。title string 方法将每个单词的首字母大写。

## 问题 11:字典流行

**答:答**

dictionary pop 的语法是 dictionary_name.pop(key，value)。pop()方法移除指定键的键值对。如果指定的值在字典中不存在，它将考虑该键并删除该元素。如果键和值都不在字典中，它不会引发错误并返回{1:2，3:4，5:6}。

## 问题 12:集合比较

**答:答**

在两个集合之间使用的比较运算符会产生一个布尔值。如果一个集合是另一个集合的子集，它将返回 true，否则将返回 false。程序将返回 true，因为 a 是 b 的子集。

## 问题 13:元组删除

**回答:D**

Del 是用于变量、列表、字典、元组等对象的关键字。在删除元组之后，在代码中呈现相同元组的打印语句。由于数据不可用，它将返回一个名称错误。

## 问题 14:列举

**答:答**

Enumerate 是一个内置函数，它遍历序列并同时获取索引和值。Enumerate()返回一对索引值及其列表元素。

## 问题 15:一行代码反转

**答案:B**

扩展切片是反转字符串的最佳单行技术。字符串的切片使用了`[begin: end: step]`语法。代码检查字符串是向前还是向后读取相同的内容。

## 问题 16:矩阵转置

**答案:C**

Zip 是一个内置函数，它从不同的列中提取数据，并将其转换为元组。该代码使用 zip 函数来转置给定的矩阵。

## 问题 17: rfind 方法

**回答:D**

rfind 方法返回字符串中最后一次出现的字符。在代码中,' o '字符出现两次，出现在第四个和第七个索引值处。rfind 返回字符 7 的最后一个事件的索引值作为输出。

## 问题 18:

**答案:C**

最后，无论代码执行中发生什么，块都将被执行。代码首先执行 try 块，然后最终返回值。

## 问题 19:关键词

**答案:B**

和其他语言一样，Python 也有一些保留字。这些保留字有一些特殊的含义和操作。在 Python 中，有三十五个关键字。

## 问题 20: Python 语言

**答:答**

流行的 Python 语言是用传统的 C 语言编写的。

这里有一个订阅链接，可以用 Medium 阅读数千篇文章。请考虑订阅并支持许多作者:

[](https://swathiarun63.medium.com/membership) [## 通过我的推荐链接加入媒体

### 作为一个媒体会员，你的会员费的一部分会给你阅读的作家，你可以完全接触到每一个故事…

swathiarun63.medium.com](https://swathiarun63.medium.com/membership)